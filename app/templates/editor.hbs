<div class="padding-vertical">
  <p>Configuración: {{model.cols}} columnas / {{model.rows}} filas

    {{#ember-remodal openButton='Editar' buttonClasses="ui button" forService=true disableNativeClose=true closeOnOutsideClick=false closeOnEscape=false}}
      <h2>Editar Grilla</h2>

      <p>Columnas: {{input value=model.cols}}</p>
      <p>Filas: {{input value=model.rows}}</p>

      <p><button class="ui button" {{action "save"}}>Guardar</button></p>

    {{/ember-remodal}}
  </p>

</div>

<div class="padding-vertical">
  {{pilas-frame currentFrame=currentFrame liveReload=liveReload path=model.path cols=model.cols rows=model.rows}}
</div>

<div class="padding-vertical">
  Cuadro actual:
  {{x-range-input min=0 max=(sub model.cols 1) step=1 value=currentFrame}}
  {{currentFrame}}
  {{!xrange-input action=(action "updateSaturation")}}
</div>


<div class="padding-vertical">
  {{#link-to "index" class="ui button"}}Volver a la pantalla inicicial{{/link-to}}
</div>
